# Secrets - docker compose reads this natively
JWT_SECRET=change_me_in_production

# SMTP (dev: mailhog on port 1025, prod: real SMTP)
SMTP_HOST=
SMTP_PORT=

# 42 OAuth (https://profile.intra.42.fr/oauth/applications)
FT_CLIENT_ID=
FT_CLIENT_SECRET=
FT_REDIRECT_URI=https://localhost:8443/auth/42/callback
FRONTEND_URL=https://localhost:8443
